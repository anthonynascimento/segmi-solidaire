{% extends "administration_admin_layout.twig" %}

{% set menu_selected = 'home' %}

{% block title %}Admin{% endblock %}

{% block content %}


    <!-- /.row -->
    <div class="row">
        <div class="col-lg-12">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Liste des annales disponibles</h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                        </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <table width="100%" class="table table-striped table-bordered table-hover" id="example">
                                <thead>
                                <tr>
                                    <th>Nom de l'annale</th>
                                    <th style="">Niveau</th>
                                    <th style="">Matière</th>
                                    <th>Fichier</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% if annales['tous'] is not empty %}
                                    {% for annales in annales['tous'] %}
                                        <tr style="" onclick="parent.location=''" style="height: 6em">
                                            <td style="">{{ annales.nom }}</td>
                                            <td style="">{{ annales.niveau }}</td>
                                            <td ><div style="">{{ annales.matiere }}</div></td>
                                            <td>{{ annales.fichier }}</td>
                                            <th class="">

                                                <a style="width: 90px" title="Visualiser l'annale" class="btn btn-success" href="{{ path('annaleDetails', {'id': annales.idAnnale }) }}">
                                                    <i class="glyphicon glyphicon-zoom-in icon-white"></i>
                                                    View
                                                </a>
                                                <a style="width: 90px" title="Modifier l'annale" class="btn btn-info" href="{{ path('modifierAnnaleAdmin', {'id': annales.idAnnale }) }}">
                                                    <i class="glyphicon glyphicon-edit icon-white"></i>
                                                    Edit
                                                </a>
                                                <a style="width: 90px" title="Supprimer l'annale" class="btn btn-danger" href="{{ path('supprimerAnnale', {'id': annales.idAnnale }) }}">
                                                    <i class="glyphicon glyphicon-trash icon-white"></i>
                                                    Delete
                                                </a>
                                            </th>
                                        </tr>

                                    {% endfor %}

                                {% else %}
                                    <strong style="color: red">Aucune annale </strong>
                                {% endif %}
                                </tbody>
                            </table>
                            <!-- /.table-responsive -->
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
        </div>
        <!-- /.col-lg-8 -->

        <!-- /.col-lg-4 -->
    </div>
    <!-- /.row -->
{% endblock %}